import React from 'react';

const Header: React.FC = () => {
  return (
    <header style={{ padding: '1rem', background: '#2e7d32', color: 'white' }}>
      <h1>Gardenia Header (Test)</h1>
    </header>
  );
};

export default Header;